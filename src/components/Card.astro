---
const cardClassName = 'card mt-7 mx-1 mb-4 md:mx-3.5 srfc-01db txt-100 br-lg';

const { class: className, element: Element = 'div', ...rest } = Astro.props;
---

<Element class={`${cardClassName} ${className || ''}`} {...rest}>
  <slot />
</Element>
