---
const { isSecondary, ...rest } = Astro.props;

const Element = isSecondary ? 'h3' : 'h1';

const className = `card-title txt-200 fs-lg md:fs-xl f-alt f-ellipsis ${
  isSecondary ? 'f-clamp m-0' : 'mt-0 mx-0 mb-1'
}`;
---

<Element class={className} {...rest}>
  <slot />
</Element>
