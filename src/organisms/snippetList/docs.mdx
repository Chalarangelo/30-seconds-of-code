import {
  Story,
  Preview,
  Props,
  Source,
  Description,
  ColorPalette,
  ColorItem,
  Meta,
} from '@storybook/addon-docs/blocks';
import SnippetList from 'organisms/snippetList';

# SnippetList

A component that display a list of snippets on a page.

## Props

<Props of={SnippetList} />

## Source

```jsx
const snippets = [...];
const paginator = {
  pageNumber: 3,
  totalPages: 7,
  baseUrl: '/list',
};

<SnippetList 
  snippetList={snippets} 
  paginator={paginator}
/>
