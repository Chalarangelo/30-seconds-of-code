import {
  Story,
  Preview,
  Props,
  Source,
  Description,
  ColorPalette,
  ColorItem,
  Meta,
} from '@storybook/addon-docs/blocks';
import CookieConsentPopup from 'molecules/cookieConsentPopup';

# CookieConsentPopup

A popup message that asks the user for consent to use cookies.

## Props

<Props of={CookieConsentPopup} />

## Source

```jsx
<CookieConsentPopup
  onAccept={(e) => console.log('Accepted!')}
  onDecline={(e) => console.log('Declined...')}
/>
```
