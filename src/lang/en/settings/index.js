import settings from 'settings/global';

const literals = {
  title: 'Settings',
  pageDescription: `Customize your ${settings.websiteName} experience.`,
  settings: {
    darkMode: 'Dark mode',
    githubLinks: 'GitHub links',
  },
};

export default literals;
