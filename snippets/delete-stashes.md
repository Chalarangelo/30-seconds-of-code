---
title: Delete all stashes
tags: stash,repository,intermediate
---

Deletes all stashes.

- Use `git stash clear` to delete all stashes.

```sh
git stash clear
```

```sh
git stash clear
# Deletes all stashes
```
